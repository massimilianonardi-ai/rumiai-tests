name: podman

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/podman.yml
      - rumiai-validate
      - lib/sh/rumiai-validate.lib.sh
      - lib/rumiai-os-target.lib
      - tests/rumiai-os/pkg-extract/**
      - tests/rumiai-os/pkg-integration/contract.test
      - tests/rumiai-os/pkg-repository-podman/**
      - tests/external/podman/**
      - validation/podman.conf
  workflow_dispatch:

permissions:
  contents: write

jobs:
  formal-validation:
    runs-on: macos-14
    timeout-minutes: 30

    steps:
      - name: Checkout current test suite
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
          path: rumiai-tests
          persist-credentials: true

      - name: Checkout product source/update point
        uses: actions/checkout@v4
        with:
          repository: massimilianonardi-ai/rumiai-os
          ref: main
          fetch-depth: 0
          path: rumiai-os
          persist-credentials: true

      - name: Normalize product origin
        shell: bash
        run: |
          git -C rumiai-os remote set-url origin https://github.com/massimilianonardi-ai/rumiai-os.git

      - name: Configure validation evidence author
        shell: bash
        run: |
          git -C rumiai-tests config user.name "github-actions[bot]"
          git -C rumiai-tests config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Run formal Podman package validation
        shell: bash
        run: |
          export TMPDIR="$RUNNER_TEMP/rumiai podman validation"
          mkdir -p "$TMPDIR"
          cd rumiai-tests
          ./rumiai-validate podman

      - name: Upload recoverable local evidence
        if: always()
        continue-on-error: true
        uses: actions/upload-artifact@v4
        with:
          name: podman-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            rumiai-tests/sessions
            rumiai-tests/validations
          if-no-files-found: ignore
          include-hidden-files: true
